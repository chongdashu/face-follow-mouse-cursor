# Face Follow Mouse Cursor - Environment Variables
# Copy this file to .env.local and fill in your values

# Replicate API Token (Required for Atlas Mode)
# Get your token at: https://replicate.com/account/api-tokens
# Free account available, pay-per-use (approximately $0.0001 per image)
# For a full atlas (121 images): ~$0.01-0.02
REPLICATE_API_TOKEN=r8_your_token_here

# Vercel KV (Required for Atlas Caching)
# These are automatically provided by Vercel when you enable KV storage
# In Vercel dashboard: Storage → Create KV Database
# Environment variables are auto-injected, no manual setup needed
# Local development: Use Vercel CLI (`vercel env pull`) to get credentials
KV_URL=your_kv_url_here
KV_REST_API_URL=your_kv_rest_api_url_here
KV_REST_API_TOKEN=your_kv_rest_api_token_here
